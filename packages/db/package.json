{
	"name": "@repo/db",
	"version": "0.0.1",
	"private": true,
	"description": "Shared database client and Prisma schema for PORES",
	"main": "index.ts",
	"types": "index.ts",
	"exports": {
		".": {
			"types": "./index.ts",
			"default": "./index.ts"
		}
	},
	"scripts": {
		"build": "prisma generate",
		"generate": "prisma generate",
		"migrate": "prisma migrate deploy",
		"seed": "tsx prisma/seed.ts"
	},
	"dependencies": {
		"@prisma/client": "^5.8.0"
	},
	"devDependencies": {
		"prisma": "^5.8.0",
		"tsx": "^4.7.0"
	}
}
